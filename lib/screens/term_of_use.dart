import 'package:flutter/material.dart';
import 'package:SignalStrengthChecker/utils/theme_changer.dart';

class TermOfUse extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Term of Use'),
      ),
      body: Center(
        child: Text('Coming Soon ...', style: TextStyle(fontSize: 24.0)),
      ),
    );
  }
}